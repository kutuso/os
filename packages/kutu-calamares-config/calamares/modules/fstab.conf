mountOptions:
  - filesystem: ext4
    options: [ defaults, noatime ]
  - filesystem: btrfs
    options: [ defaults, noatime, compress=zstd ]
  - filesystem: fat32
    options: [ fmask=0077, dmask=0077 ]
