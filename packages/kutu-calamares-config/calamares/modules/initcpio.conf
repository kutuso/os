# mkinitcpio runs in the target via the initcpio module; the hook list is
# derived from the actual partition layout by initcpiocfg (block, filesystems,
# encrypt/lvm2/resume, microcode).
kernel: linux
be_unsafe: false
